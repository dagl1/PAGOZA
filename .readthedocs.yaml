version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.11"

  jobs:
    pre_install:
      - pip install uv

    install:
      - uv pip install -e .
      - uv pip install -r docs/requirements.txt

sphinx:
  configuration: docs/source/conf.py
